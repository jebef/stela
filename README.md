# Stela 
A lightweight blog template with minimal dependencies. Posts are written in markdown and 
parsed with a minified version of markdown-it (included in scripts). Legacy hash routing, 
simple styling, reactive layouts. 

